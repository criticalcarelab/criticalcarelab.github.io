# site properties and page defaults
title: Critical Care Research Lab
subtitle: 
description: We study the microvascular physiology of critical illness to improve the diagnosis, monitoring, and treatment of patients in intensive care. Our work integrates biomedical optics, advanced signal analysis, and translational animal models to bridge the gap between bench and bedside.
header: images/background.png
footer: images/background.png
proofer: false

# site social media and other links
links:
  email: asher.mendelson@umanitoba.ca
  orcid: 0000-0003-2108-4752
  google-scholar: LzC6g6YAAAAJ&hl
  github: criticalcarelab
  twitter: AsherMendelson

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
